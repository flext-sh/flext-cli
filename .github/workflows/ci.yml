# Generated by scripts/github/sync_workflows.py - DO NOT EDIT
name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.13"

      - name: Install Poetry
        uses: snok/install-poetry@76e04a911780d5b312d89783f7b1cd627778900a
        with:
          virtualenvs-create: false
          installer-parallel: true

      - name: Resolve branch
        id: branch
        run: |
          BRANCH="${FLEXT_CORE_BRANCH:-${GITHUB_HEAD_REF:-0.12.0-dev}}"
          if [ -z "${BRANCH}" ] || [ "${BRANCH}" = "HEAD" ]; then
            echo "Unable to resolve branch for flext-core sync." >&2
            exit 1
          fi
          echo "branch=${BRANCH}" >> "$GITHUB_OUTPUT"

      - name: Setup
        env:
          FLEXT_CORE_BRANCH: ${{ steps.branch.outputs.branch }}
        run: make setup

      - name: Check
        env:
          FLEXT_CORE_BRANCH: ${{ steps.branch.outputs.branch }}
        run: make check

      - name: Test
        env:
          FLEXT_CORE_BRANCH: ${{ steps.branch.outputs.branch }}
        run: make test

      - name: Validate
        env:
          FLEXT_CORE_BRANCH: ${{ steps.branch.outputs.branch }}
        run: make validate
